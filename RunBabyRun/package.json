{
  "name": "run-baby-run",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "extract": "node scripts/extract-assets.mjs",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:pages": "playwright test --config playwright.pages.config.js",
    "test:coop": "playwright test --config playwright.coop.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "peer": "^1.0.2",
    "pngjs": "^7.0.0",
    "vite": "^6.0.7",
    "vitest": "^3.0.5"
  },
  "dependencies": {
    "peerjs": "^1.5.5",
    "qrcode": "^1.5.4"
  }
}
